#
#	Makefile for config
#

@SET_MAKE@

SHELL = /bin/sh

include @common_makefile@


all: mppsscp-all storcmtscp-all

install: install-bin

install-bin:

mppsscp-all:

storcmtscp-all: 

clean:
	rm -rf $(TRASH)

distclean:
	rm -rf $(DISTTRASH) Makefile.def

dependencies:
